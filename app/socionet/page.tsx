import SocionetApp from '@/components/SocionetApp';

export default function SocionetPage() {
  return (
    <main className="container">
      <h1>SOCIONET Super App</h1>
      <p>Installable, local-first social super app prototype with identity, feed, messaging, discovery, AI, privacy controls, and settings.</p>
      <SocionetApp />
    </main>
  );
}
